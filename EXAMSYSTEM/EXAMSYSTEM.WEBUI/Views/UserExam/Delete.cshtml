@model EXAMSYSTEM.CORE.Models.UserExam

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-12 stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <tbody>
                        <tr>
                            <td>@Html.DisplayNameFor(model => model.Subject.SubjectName)</td>
                            <td>@Html.DisplayFor(model => model.Subject.SubjectName)</td>
                        </tr>
                        <tr>
                            <td>@Html.DisplayNameFor(model => model.User.Username)</td>
                            <td>@Html.DisplayFor(model => model.User.Username)</td>
                        </tr>
                        <tr>
                            <td>@Html.DisplayNameFor(model => model.Score)</td>
                            <td>@Html.DisplayFor(model => model.Score)</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div>
                <form asp-action="Delete">
                    <div class="form-group">
                        <input type="hidden" asp-for="UserExamId" />
                        <button type="submit" class="btn btn-danger mr-2">Delete</button>
                        <button asp-action="Index" class="btn btn-light">Cancel</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>